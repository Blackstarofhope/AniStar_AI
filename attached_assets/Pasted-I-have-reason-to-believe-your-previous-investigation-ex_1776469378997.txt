I have reason to believe your previous investigation examined the wrong database. I see Neon-style connection drops (“administrator command” terminations) which are characteristic of Neon, not Replit Helium Postgres. Yet you reported DATABASE_URL points at Helium. Something doesn’t add up.
Investigate the actual live DB connection — show me raw command output, do not synthesize conclusions. For each step, paste the literal command you ran and the literal output:
	1.	In the currently-running dev server process, add a one-time log that prints process.env.DATABASE_URL at runtime (redact the password but show the full host). I want to see what URL the running server is actually using, not what .env files or env var lookups might suggest. Use the shell or inject a temporary console.log into the server entry point, restart it, and show me the log line.
	2.	From the shell, run the command env | grep -i database and show me the literal output. Also run printenv DATABASE_URL and show me what it returns.
	3.	Check if there are multiple DB URLs in play — search the codebase for every env var that contains the word DATABASE, NEON, POSTGRES, or PG. Show me every match with filename and line.
	4.	Check what host the live connection is going to by running SELECT inet_server_addr(), inet_server_port(), current_database(), current_user through the currently-connected pg client inside the running server. Or, if that’s not feasible, run the equivalent query via psql $DATABASE_URL -c 'SELECT inet_server_addr(), current_database();' and show the output. If the host resolves to a Neon address (*.neon.tech, *.aws.neon.tech), then we’re hitting Neon. If it resolves to a local/Replit IP, we’re hitting Helium.
	5.	Check the deployment’s env vars separately. I know you said the deployment has a different configuration. Run the Replit CLI or dashboard command to show deployment env vars and paste the output. What is the deployment’s DATABASE_URL host?
For every step, paste the actual command and actual output. Do not summarize, do not synthesize. I need to see the raw data.